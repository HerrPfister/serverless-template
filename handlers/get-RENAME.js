/*
  [GET]: This handler will be used to get _specific data_ from a specific resource based provided parameters
*/
module.exports.handler = async (event) => {
  /* 
    TODO: Add handler code that utilizes a controller, e.g.
    
    const { body, headers, pathParameters, queryStringParameters } = event;

    const response = await controller.get();

    return await response;
  */

  // TODO: Remove the following when handler code is added:
  return {
    statusCode: 204
  };
};
